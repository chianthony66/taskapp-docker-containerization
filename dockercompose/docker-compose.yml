services:
  db:
    image: postgres:15-alpine
    container_name: taskapp-dc
    environment:
      POSTGRES_USER: taskapp_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: taskapp
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - taskapp-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskapp_user -d taskapp"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./taskapp_backend_cicd
      dockerfile: Dockerfile
    container_name: taskapp-backendc
    environment:
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      DATABASE_USER: taskapp_user
      DATABASE_PASSWORD: ${DB_PASSWORD}
      DATABASE_NAME: taskapp
      SECRET_KEY: ${SECRET_KEY}
      FLASK_ENV: production
      WORKERS: 4
    ports:
      - "5000:5000"
    networks:
      - taskapp-network
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./taskapp_frontend_cicd
      dockerfile: Dockerfile
    container_name: taskapp-frontendc
    environment:
      VITE_API_URL: http://localhost:5000/api
    ports:
      - "8080:80"
    networks:
      - taskapp-network
    depends_on:
      - backend
    restart: unless-stopped

networks:
  taskapp-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
